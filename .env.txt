# ============================================================
# SR Technologie — Configuration Serveur d'inscription
# Renommez ce fichier en .env et remplissez les valeurs
# ============================================================

# ── SMTP (Gmail recommandé) ──────────────────────────────────
# Pour Gmail, activez les "App Passwords" dans les paramètres
# de sécurité de votre compte Google.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=srtechnologie0@gmail.com
SMTP_PASS=lqsp lu rm ofzi hsru u

# ── Email administrateur ─────────────────────────────────────
# Adresse qui reçoit une copie de chaque inscription + preuve de paiement
ADMIN_EMAIL=srtechnologie0@gmail.com

# ── Serveur ──────────────────────────────────────────────────
PORT=3000
